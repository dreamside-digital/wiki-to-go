<div class="container">
  <div class="article-list col-md-10 col-centered">
  <h2 style="text-align: center;"><%= @book.title %></h2>
    <ul class="list-unstyled"><% @articles.each do |article| %>
      <div class="article-item">
        <h3><%= article.title %></h3>
        <%= content_tag :div, id: article.pageid do %> 
          <%= content_tag :p, class: "article-intro-text" do %>
            <%= article.intro %>
          <% end %>
        <% end %>
        <a href="<%= article.url %>" class="btn-open-wikipedia btn" target="blank"> 
          <%= article.url %> <span class="glyphicon glyphicon-link" aria-hidden="true"></span>
        </a>
      </div>
    <% end %>
    </ul>
  </div>
</div>