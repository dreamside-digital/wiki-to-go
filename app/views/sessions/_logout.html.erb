<ul class="nav navbar-nav navbar-right">
<!-- 	<li class= "navbar-text menu-white-text"> Welcome, <%= current_user.name %></li> -->
  <li class="dropdown">
    <a href="#" class="dropdown-toggle menu-white-text" data-toggle="dropdown" aria-expanded="false"> <%= current_user.name %>'s Wiki Collections <span class="caret"></span></a>
      <ul class="dropdown-menu"><% @user_wikis.each do |wiki| %>
          <li>
            <a href="<%= user_book_path(@user, wiki) %>">
            <span class="glyphicon glyphicon-time" aria-hidden="true"></span>
            <%= wiki.title %></a>
          </li>
        <% end %>
        <li class="divider"></li>
        <li><%= link_to "See them all!", user_books_path(current_user.id) %></li>
      </ul>
  </li>
  <li class="dropdown">
    <a href="#" class="dropdown-toggle menu-white-text" data-toggle="dropdown" role="button" aria-expanded="false"> Profile <span class="caret"></span></a>
      <ul class="dropdown-menu" role="menu">
        <li><%= link_to "View my profile", user_path(current_user.id) %></li>
        <li><%= link_to "Edit my profile", edit_user_path(current_user.id) %></li>
      </ul>
  </li>
  <li><%= link_to "Logout", logout_path, method: :delete, class: "menu-white-text" %></li>
</ul>